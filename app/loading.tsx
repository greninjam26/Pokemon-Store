import LoadingSpinner from "@/components/shared/loading-spinner";

function LoadingPage() {
	return (
		<LoadingSpinner
			className="wrapper"
			label="Loading the Pokemon Store..."
		/>
	);
}

export default LoadingPage;
